# fullstack-todo-app
A modular full-stack Todo application built with Express.js and MongoDB on the backend and vanilla JavaScript (ES modules) on the frontend. Implements RESTful CRUD operations, server-side validation with express-validator, structured MVC architecture, and async client–server communication using Fetch API.
